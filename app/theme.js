// theme.js

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6930C3',
    },
    secondary: {
      main: '#64DFDF',
    },
    text: {
      primary: '#FFFFFF',
    },
    background: {
      default: '#0E103D',
    },
  },
});

export default theme;
